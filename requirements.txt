# requirements.txt для airstorebot / fake ADS-B проекта

# Базовые (если добавишь в будущем)
requests>=2.28.0      # для работы с веб-API, если понадобится
numpy>=1.24.0         # для математических расчётов траекторий
matplotlib>=3.7.0     # для отрисовки пути самолёта / графиков
pygame>=2.5.0         # для простой 2D-визуализации карты

# Опционально, если будешь работать с реальными данными ADS-B
pyModeS>=1.0.0        # декодирование/генерация Mode-S сообщений (ADS-B)
geopy>=2.4.0          # расчёт расстояний/координат
